import { cTopLeading, HStack, UIController, UIRouteOutlet, UIScene } from '@tuval/forms';




export class SecurityLayoutController extends UIController {

    public LoadView(): any {
        return (

            UIRouteOutlet().width('100%').height('100%')

        )
    }
}